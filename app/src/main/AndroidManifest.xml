<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Documents are stored in /sdcard/Drafting Table/ so they survive
        uninstall and are browsable from the Android Files app, matching
        the pattern of Clip Studio / Sketchbook / Wacom Canvas. Writing
        to a top-level shared-storage folder on Android 11+ requires
        the "All files access" special permission, which the user grants
        via system settings (not a runtime dialog). The app prompts on
        first launch.
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.DrawingApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:supportsRtl="true">

        <!-- Export "Share": staged files in cacheDir/exports go out as
             content:// URIs (see res/xml/export_paths.xml). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/export_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|uiMode|density|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
